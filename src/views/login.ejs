<%- include('./includes/head') %>
<%- include('./includes/nav') %>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-lg-10 col-md-12">
      <h1 class="display-4 text-center mb-4">Criar conta ou Entrar</h1>
      <p class="text-center lead mb-4">Faça login ou crie sua conta abaixo</p>

      <%- include('./includes/messages') %>
      
      <div class="row">
        <!-- Coluna para cadastro -->
        <!--
        <div class="col-lg-6 mb-4 mb-lg-0">
          <h4 class="mb-3">Faça seu cadastro</h4>
          <p>Se você ainda não tem conta, preencha o formulário abaixo:</p>
          <form action="/login/register" method="POST">
            <input type="hidden" name="_csrf" value=<%= csrfToken %>>
            <div class="form-group">
              <label>Seu e-mail</label>
              <input type="email" class="form-control" name="email" required>
            </div>
            <div class="form-group">
              <label>Sua Senha</label>
              <input type="password" class="form-control" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary btn-lg btn-block">Criar minha conta</button>
          </form>
        </div>
        -->

        <!-- Coluna para login -->
        <div class="col-lg-6">
          <h4 class="mb-3">Faça seu Login</h4>
          <p>Se você já tem uma conta, faça login abaixo:</p>
          <form action="/login/login" method="POST">
            <input type="hidden" name="_csrf" value=<%= csrfToken %>>
            <div class="form-group">
              <label>Seu e-mail</label>
              <input type="email" class="form-control" name="email" required>
            </div>
            <div class="form-group">
              <label>Sua Senha</label>
              <input type="password" class="form-control" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary btn-lg btn-block">Entrar</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('./includes/footer') %>
